<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<%= button_to "Edit", edit_article_path(@article), :method => :get %>
<%= button_to "Delete", article_path(@article), :method => :detete, form: { data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } } %>
<%= button_to "Home", root_path, :method => :get %>